name: Labeler
on:
  pull_request:
    branches:
    - main
jobs:
  label:

    runs-on: ubuntu-latest
    permissions:
      contents: read
      pull-requests: write

    steps:
    - uses: label/labeler@v4
      with:
        repo-token: "${{ secrets.GITHUB_TOKEN }}"
  
